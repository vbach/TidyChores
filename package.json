{
  "name": "capstone-project-vbach",
  "version": "1.0.0",
  "scripts": {
    "reactjs": "cd reactjs && npm start",
    "api": "cd api; nodemon server.js",
    "server": "nodemon server.js",
    "dev": "concurrently --raw \"npm run server\" \"npm run reactjs\"",
    "start": "node server.js",
    "heroku-postbuild": "cd reactjs && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
